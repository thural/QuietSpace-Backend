# Staging profile: uses Mailpit SMTP (from base) with MySQL/Flyway (from base).
# No overrides needed — base application.yml already provides the
# deployed defaults that both staging and production share.
